services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: discord-global-chat-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
